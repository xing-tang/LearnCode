1. 仿照ArrayList编写一个属于自己的ArrayList集合，并且具备如下条件
- 无参构造函数。
- 有参构造函数，默认容量为10。
- 获取数组中的元素个数。
- 获取数组的容量大小。
- 判断数组是否为空。
- 向数组中某个位置添加一个新的元素。
- 向数组中所有元素前添加一个新的元素。
- 向数组中所有元素后添加一个新的元素。
- 获取某个索引坐标的元素值。
- 获取第一个索引坐标的元素值。
- 获取最后一个索引坐标的元素值。
- 修改index索引坐标的元素e。
- 检查当前数组是否包含元素e。
- 查找数组中元素e的索引坐标。
- 从数组中删除index位置的元素,并返回删除的元素值。
- 从数组中删除第一个元素。
- 从数组中删除最后一个元素。
- 从数组中删除元素e，但是这里注意，这里删除的e只是元素e第一次出现在数组中的位置，如果后续有相同的元素e则不会删除。
- 动态数组自动扩容，扩容大小为1.5倍。
- 获取String类型结果。